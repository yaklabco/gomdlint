# Stave configuration for gomdlint
# https://github.com/yaklabco/stave

# Speed up compilation for git hooks
hash_fast: true

# Git hooks - run targets on git events
hooks:
  pre-commit:
    - target: lint:fmtcheck
    - target: lint:ci
  pre-push:
    - target: test
