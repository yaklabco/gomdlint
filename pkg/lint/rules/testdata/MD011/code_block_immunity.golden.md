# Code Block Immunity

The following reversed link syntax is inside a code block:

```markdown
This has (Click here)[https://example.com] reversed link syntax.
Another (see docs)[https://docs.example.com] reversed link syntax.
```

Normal text after the code block.
