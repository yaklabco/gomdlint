# List Test

- This is a very long list item that exceeds the default line length limit and should be wrapped at a word boundary when
  the autofix is applied.

1. This is a very long numbered list item that exceeds the default line length limit and should be wrapped properly when
   the autofix runs on it.
