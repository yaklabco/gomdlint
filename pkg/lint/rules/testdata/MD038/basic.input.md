# No Space in Code Test

This has `  extra leading spaces` in code.

This has `extra trailing spaces  ` in code.

This has `  spaces on both sides  ` in code.

Normal `code` is fine.

Single space padding ` backtick ` is allowed.
