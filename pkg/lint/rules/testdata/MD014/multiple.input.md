# Multiple Code Blocks

First block:

```bash
$ npm install
$ npm run build
```

Second block:

```sh
$ docker build -t app .
$ docker push app
```

Third block with output (should not flag):

```bash
$ ls -la
total 42
drwxr-xr-x 5 user staff 160 Jan 1 00:00 .
```
