# Homebrew dependencies for the gomdlint project
# Usage:
#   brew bundle install --no-lock
#   brew bundle cleanup --force   # optional: remove anything not in this Brewfile

# Go-related formulae
brew "go"
brew "gofumpt"
brew "goimports"
brew "golangci-lint"
brew "golangci-lint-langserver"
brew "goreleaser"

# Go-based tools (public only - private modules installed separately with auth)
go "github.com/caarlos0/svu"

